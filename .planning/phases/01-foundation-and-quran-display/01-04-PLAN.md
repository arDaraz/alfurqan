---
phase: 01-foundation-and-quran-display
plan: 04
type: execute
wave: 3
depends_on: ["01-02", "01-03"]
files_modified:
  - app/onboarding.tsx
  - app/_layout.tsx
  - src/components/onboarding/OnboardingScreen.tsx
  - src/components/onboarding/OnboardingDots.tsx
autonomous: false
requirements:
  - UI-04
  - UI-01

must_haves:
  truths:
    - "First-time user sees a 3-screen swipeable onboarding walkthrough before the home screen"
    - "Onboarding shows bilingual content (English + Arabic) with correct copy from UI-SPEC"
    - "User can swipe between onboarding screens with spring animation and page dots"
    - "Screen 3 has a Get Started button that completes onboarding and navigates to home"
    - "After onboarding, the app never shows onboarding again (persisted flag in MMKV)"
    - "Returning user goes directly to home screen (skips onboarding)"
    - "All interactive elements, screens, and flows from Plans 01-03 work together correctly"
  artifacts:
    - path: "app/onboarding.tsx"
      provides: "Onboarding screen route with 3-screen swipeable flow"
      min_lines: 40
    - path: "src/components/onboarding/OnboardingScreen.tsx"
      provides: "Single onboarding slide with illustration, heading, body in bilingual"
      min_lines: 40
    - path: "src/components/onboarding/OnboardingDots.tsx"
      provides: "Page indicator dots for 3 onboarding screens"
      min_lines: 15
  key_links:
    - from: "app/_layout.tsx"
      to: "src/stores/readingStore.ts"
      via: "check hasCompletedOnboarding to route"
      pattern: "hasCompletedOnboarding"
    - from: "app/onboarding.tsx"
      to: "src/stores/readingStore.ts"
      via: "completeOnboarding on Get Started press"
      pattern: "completeOnboarding"
    - from: "app/onboarding.tsx"
      to: "app/(tabs)/index.tsx"
      via: "router.replace after onboarding"
      pattern: "router\\.replace"
---

<objective>
Build the 3-screen bilingual onboarding flow and wire it into the app routing so first-time users see onboarding before the home screen, then verify the complete Phase 1 experience end-to-end on a device.

Purpose: First-time users need clear onboarding explaining how the app works (UI-04) with a polished design (UI-01). This is also the final integration checkpoint where all Phase 1 features must work together.

Output: Complete onboarding flow, integrated routing, and a human-verified end-to-end experience.
</objective>

<execution_context>
@/Users/ahmeddaraz/VibeCoding/tasmi/.claude/get-shit-done/workflows/execute-plan.md
@/Users/ahmeddaraz/VibeCoding/tasmi/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-foundation-and-quran-display/01-CONTEXT.md
@.planning/phases/01-foundation-and-quran-display/01-UI-SPEC.md
@.planning/phases/01-foundation-and-quran-display/01-RESEARCH.md
@.planning/phases/01-foundation-and-quran-display/01-01-SUMMARY.md
@.planning/phases/01-foundation-and-quran-display/01-02-SUMMARY.md
@.planning/phases/01-foundation-and-quran-display/01-03-SUMMARY.md

<interfaces>
<!-- From Plan 01 stores and utils -->

From src/stores/readingStore.ts:
```typescript
export const useReadingStore: {
  hasCompletedOnboarding: boolean;
  completeOnboarding: () => void;
}
```

From src/utils/locale.ts:
```typescript
export type AppLanguage = 'ar' | 'en';
export function getDeviceLanguage(): AppLanguage;
```

From src/stores/settingsStore.ts:
```typescript
export const useSettingsStore: {
  language: AppLanguage;
}
```

From src/constants/theme.ts:
```typescript
export const theme: {
  colors: { primary: '#0D7377'; accent: '#C9A84C'; background: '#FAF8F2'; text: '#1A1A2E'; textSecondary: '#6B7280' };
  spacing: { md: 16; lg: 24; xl: 32; '3xl': 64 };
  typography: { display: { size: 28; weight: '700' }; body: { size: 18; weight: '400' }; };
  fonts: { arabic: 'KFGQPC-Uthmani' };
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: OnboardingScreen, OnboardingDots components and onboarding route</name>
  <files>
    src/components/onboarding/OnboardingScreen.tsx, src/components/onboarding/OnboardingDots.tsx,
    app/onboarding.tsx, app/_layout.tsx
  </files>
  <read_first>
    .planning/phases/01-foundation-and-quran-display/01-UI-SPEC.md (Onboarding screen layout wireframe, copywriting contract for all 3 screens, loading and transition states for onboarding swipe, accessibility requirements for reduced motion),
    .planning/phases/01-foundation-and-quran-display/01-CONTEXT.md (locked decisions: 3-screen swipeable walkthrough content, bilingual text default language from locale, microphone permission NOT asked during onboarding),
    src/stores/readingStore.ts (completeOnboarding action, hasCompletedOnboarding state),
    src/stores/settingsStore.ts (language preference),
    src/utils/locale.ts (getDeviceLanguage function),
    src/constants/theme.ts (theme tokens),
    app/_layout.tsx (current root layout to update routing logic)
  </read_first>
  <action>
    **Create `src/components/onboarding/OnboardingScreen.tsx`:**

    Props:
    ```typescript
    {
      headingEn: string;
      headingAr: string;
      bodyEn: string;
      bodyAr: string;
      illustrationIcon: string; // MaterialCommunityIcons name
      isLastScreen: boolean;
      onGetStarted?: () => void;
    }
    ```

    Layout per UI-SPEC Onboarding wireframe:
    - Full screen, background #FAF8F2 (cream).
    - Top area (200px height): Centered illustration. For Phase 1, use a large MaterialCommunityIcons icon (size 120) with gold (#C9A84C) accent color as placeholder for custom illustrations. Screen 1: 'book-open-variant', Screen 2: 'microphone', Screen 3: 'rocket-launch'.
    - Below illustration, centered, 32px (xl) horizontal padding:
      - English heading: Display size (28px), weight 700, #1A1A2E, centered. System font (not Arabic font).
      - Arabic heading: Display size (28px), weight 700, #1A1A2E, centered, fontFamily 'KFGQPC-Uthmani', `writingDirection: 'rtl'`. 8px below English heading.
      - English body: Body size (18px), weight 400, #6B7280, centered, 16px (md) top margin. System font.
      - Arabic body: Body size (18px), weight 400, #6B7280, centered, fontFamily 'KFGQPC-Uthmani', `writingDirection: 'rtl'`. 8px below English body.
    - If `isLastScreen === true`:
      - "Get Started" / "ابدأ الآن" button at bottom: #0D7377 background, white text, Body size (18px), 44px height, 16px horizontal padding, 12px border-radius, centered horizontally. 32px (xl) bottom padding from safe area.
      - On press: call `onGetStarted()`.
    - If `isLastScreen === false`:
      - Swipe hint text at bottom: "Swipe to continue" in Label size (14px), #9CA3AF, centered. (UI-SPEC notes "swipe hint on 1-2")

    Language ordering: If device language is Arabic, show Arabic text FIRST (above English). Otherwise English first. Read from settingsStore.language or call getDeviceLanguage().

    **Create `src/components/onboarding/OnboardingDots.tsx`:**

    Props: `{ total: number; active: number }`

    Layout per UI-SPEC:
    - Horizontal row of dots, centered.
    - Active dot: 10px diameter, #0D7377 (teal).
    - Inactive dot: 8px diameter, #9CA3AF (gray-400).
    - Spacing between dots: 8px.
    - Animate active dot scale and color transition using react-native-reanimated `withSpring`.

    **Create `app/onboarding.tsx`:**

    Content per UI-SPEC copywriting contract (exact copy):

    Screen 1:
    - headingEn: "Your Digital Memorization Partner"
    - headingAr: "شريكك الرقمي في الحفظ"
    - bodyEn: "Practice your Quran memorization anytime, anywhere — receive instant correction like a real teacher."
    - bodyAr: "تدرّب على حفظ القرآن في أي وقت ومكان — تصحيح فوري كالمعلم الحقيقي"
    - illustrationIcon: "book-open-variant"

    Screen 2:
    - headingEn: "Recite. We Listen. Instant Feedback."
    - headingAr: "اقرأ. نسمع. تصحيح فوري."
    - bodyEn: "Select any surah and ayah range. Recite from memory while the app follows along word by word."
    - bodyAr: "اختر أي سورة ومجموعة آيات. اقرأ من حفظك والتطبيق يتابعك كلمة بكلمة."
    - illustrationIcon: "microphone"

    Screen 3:
    - headingEn: "Begin Your Journey"
    - headingAr: "ابدأ رحلتك"
    - bodyEn: "Choose a surah to start reading. Your progress is saved automatically."
    - bodyAr: "اختر سورة لتبدأ القراءة. يُحفظ تقدمك تلقائيًا."
    - illustrationIcon: "rocket-launch"
    - isLastScreen: true

    Implementation:
    1. Use ScrollView with `horizontal`, `pagingEnabled`, `showsHorizontalScrollIndicator={false}` for swipe navigation.
    2. Track current page index via `onMomentumScrollEnd` event: `Math.round(event.nativeEvent.contentOffset.x / screenWidth)`.
    3. Pass active index to OnboardingDots.
    4. On "Get Started" press:
       ```typescript
       readingStore.getState().completeOnboarding();
       router.replace('/(tabs)');
       ```
    5. Swipe animation: Use react-native-reanimated for spring-based page transitions. Alternatively, rely on ScrollView's native paging which provides smooth swipe on both platforms.
    6. Respect reduced motion: If reduced motion enabled, use instant transitions (no spring easing).

    **Update `app/_layout.tsx`:**

    Ensure the root layout properly handles onboarding routing:
    1. Check `useReadingStore.getState().hasCompletedOnboarding`.
    2. Use Expo Router's `Redirect` component:
       ```typescript
       const { hasCompletedOnboarding } = useReadingStore();

       if (!fontsLoaded) return null; // or SplashScreen

       return (
         <Stack screenOptions={{ headerShown: false }}>
           {!hasCompletedOnboarding ? (
             <Stack.Screen name="onboarding" options={{ gestureEnabled: false }} />
           ) : null}
           <Stack.Screen name="(tabs)" />
           <Stack.Screen name="surah/[id]" options={{ headerShown: true }} />
         </Stack>
       );
       ```
    3. Use `Redirect` component to route first-time users:
       ```typescript
       if (!hasCompletedOnboarding) {
         return <Redirect href="/onboarding" />;
       }
       ```
    4. After completing onboarding, `router.replace('/(tabs)')` ensures user can't swipe back to onboarding.
    5. On subsequent launches, hasCompletedOnboarding is true (persisted in MMKV), so onboarding is skipped entirely.
  </action>
  <verify>
    <automated>npx jest --passWithNoTests</automated>
  </verify>
  <acceptance_criteria>
    - src/components/onboarding/OnboardingScreen.tsx contains "Your Digital Memorization Partner" (screen 1 heading from UI-SPEC)
    - src/components/onboarding/OnboardingScreen.tsx contains "شريكك الرقمي في الحفظ" (Arabic heading)
    - src/components/onboarding/OnboardingScreen.tsx contains 'KFGQPC-Uthmani' for Arabic text
    - src/components/onboarding/OnboardingScreen.tsx contains "Get Started" and '#0D7377' for CTA button
    - src/components/onboarding/OnboardingScreen.tsx contains "writingDirection: 'rtl'" for Arabic text
    - src/components/onboarding/OnboardingDots.tsx contains '#0D7377' (active dot) and '#9CA3AF' (inactive dot)
    - app/onboarding.tsx contains all 3 screen headings: "Your Digital Memorization Partner", "Recite. We Listen. Instant Feedback.", "Begin Your Journey"
    - app/onboarding.tsx contains all 3 Arabic headings: "شريكك الرقمي في الحفظ", "اقرأ. نسمع. تصحيح فوري.", "ابدأ رحلتك"
    - app/onboarding.tsx contains 'completeOnboarding' call
    - app/onboarding.tsx contains "router.replace" navigation to tabs
    - app/onboarding.tsx contains 'pagingEnabled' or equivalent swipe pagination
    - app/_layout.tsx contains 'hasCompletedOnboarding' check
    - app/_layout.tsx contains 'Redirect' or conditional routing to onboarding
    - app/_layout.tsx contains 'gestureEnabled: false' on onboarding screen (prevent back swipe)
  </acceptance_criteria>
  <done>
    3-screen bilingual onboarding flow is complete with exact UI-SPEC copy, swipeable navigation with page dots, "Get Started" button on screen 3 that persists completion flag and navigates to home, and root layout correctly routes first-time users to onboarding and returning users to home.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: End-to-end Phase 1 verification on device</name>
  <files>No files modified — verification only</files>
  <action>
    Build the app using `npx expo run:ios` or `npx expo run:android` (dev client) and verify the complete Phase 1 experience on a physical device or emulator. Run through the full verification checklist below.
  </action>
  <verify>
    <automated>npx jest</automated>
  </verify>
  <done>Human has verified all Phase 1 features work correctly on device and approved the experience.</done>
  <what-built>
    Complete Phase 1 application: Expo SDK 55 project with full Quran database (114 surahs, 6236 ayahs), home screen with surah/juz browsing and search, Quran reader with Uthmani Arabic text rendering and ornamental elements, ayah range selection, auto-bookmark, and 3-screen bilingual onboarding.
  </what-built>
  <how-to-verify>
    Build and run on a physical device (or emulator for initial check):

    1. **Onboarding (first launch):**
       - Clear app data or fresh install
       - App should show 3-screen onboarding
       - Verify bilingual text (English + Arabic) is visible on each screen
       - Swipe through all 3 screens, verify page dots update
       - Tap "Get Started" on screen 3 -> navigates to home

    2. **Home screen:**
       - Verify 114 surahs appear in scrollable list
       - Each surah shows: number in gold diamond, Arabic name, English name, ayah count, revelation type
       - Tap "Juz" tab -> shows 30 juz items
       - Tap "Surah" tab -> back to surah list
       - Type "baqara" in search -> filters to Al-Baqarah
       - Clear search -> all surahs return
       - Search "1" -> shows Al-Fatiha (number match)

    3. **Quran reader:**
       - Tap Al-Fatiha -> reader opens with 7 ayahs, no separate Bismillah
       - Verify Arabic text is right-aligned, readable, diacritics visible
       - Verify ornamental surah header with gold frame
       - Verify ayah end markers with Arabic-Indic numerals in gold
       - Go back, tap Al-Baqarah -> Bismillah appears, surah header shows, 286 ayahs load
       - Check surah 9 (At-Tawbah) -> no Bismillah

    4. **Ayah range selection:**
       - In any surah, tap an ayah -> teal highlight, selection bar slides up
       - Tap another ayah -> range highlights, "Start Practice" button appears
       - Tap "Start Practice" -> toast shows "Recitation coming soon"
       - Tap "Clear Selection" -> highlights removed

    5. **Auto-bookmark:**
       - Open a long surah (Al-Baqarah), scroll down
       - Go back to home, then reopen Al-Baqarah -> position restored
       - Verify Resume Reading FAB appears on home screen
       - Tap FAB -> navigates to last-read surah

    6. **Returning user:**
       - Close and reopen app -> goes directly to home (no onboarding)

    7. **Visual quality:**
       - Colors match: teal buttons, gold ornaments, cream background
       - Arabic font is KFGQPC Uthmani (elegant mushaf-style, not generic)
       - Diacritics do not clip or collide between lines
       - Overall feel is polished and reverent
  </how-to-verify>
  <resume-signal>Type "approved" if everything works, or describe specific issues to fix</resume-signal>
</task>

</tasks>

<verification>
- Full app flow: onboarding -> home -> reader -> selection -> bookmark
- All 6 phase requirements (QTEXT-01 through QTEXT-04, UI-01, UI-04) are addressed
- Arabic text renders correctly with Uthmani diacritics
- Visual design matches UI-SPEC
- `npx jest` passes full test suite
</verification>

<success_criteria>
- First-time user sees bilingual onboarding, taps Get Started, reaches home screen
- User can browse 114 surahs and 30 juz with search
- User can read any surah with proper Arabic rendering and ornamental elements
- User can select ayah ranges via tap-to-select
- App auto-saves and restores last-read position
- Human approves the end-to-end experience on device
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-and-quran-display/01-04-SUMMARY.md`
</output>
