---
phase: 01-foundation-and-quran-display
plan: 02
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - app/(tabs)/index.tsx
  - app/(tabs)/_layout.tsx
  - src/components/home/SurahListItem.tsx
  - src/components/home/JuzListItem.tsx
  - src/components/home/SearchBar.tsx
  - src/components/home/TabBar.tsx
  - src/components/home/ResumeReadingFAB.tsx
  - src/components/ui/EmptySearchResult.tsx
  - src/components/ui/ErrorState.tsx
autonomous: true
requirements:
  - QTEXT-02
  - UI-01

must_haves:
  truths:
    - "User sees all 114 surahs in a scrollable list on the home screen"
    - "User can switch between Surah and Juz tabs to browse by either dimension"
    - "User can type in the search bar and see the surah list filter in real-time"
    - "User can tap a surah to navigate to the Quran reader screen"
    - "User sees a Resume Reading FAB when a last-read position exists"
  artifacts:
    - path: "app/(tabs)/index.tsx"
      provides: "Home screen with surah/juz lists, search, and navigation"
      min_lines: 50
    - path: "src/components/home/SurahListItem.tsx"
      provides: "Surah card with number diamond, Arabic name, English name, metadata"
      min_lines: 30
    - path: "src/components/home/JuzListItem.tsx"
      provides: "Juz card with number and start position"
      min_lines: 20
    - path: "src/components/home/SearchBar.tsx"
      provides: "Search input with debounced filtering"
      min_lines: 20
    - path: "src/components/home/TabBar.tsx"
      provides: "Surah/Juz segmented control"
      min_lines: 20
    - path: "src/components/home/ResumeReadingFAB.tsx"
      provides: "Floating action button for last-read navigation"
      min_lines: 15
  key_links:
    - from: "app/(tabs)/index.tsx"
      to: "src/hooks/useSurahList.ts"
      via: "hook call for surah data"
      pattern: "useSurahList"
    - from: "app/(tabs)/index.tsx"
      to: "src/hooks/useSearch.ts"
      via: "hook call for search filtering"
      pattern: "useSearch"
    - from: "src/components/home/SurahListItem.tsx"
      to: "app/surah/[id].tsx"
      via: "router.push navigation on press"
      pattern: "router\\.push.*surah"
    - from: "src/components/home/ResumeReadingFAB.tsx"
      to: "src/stores/readingStore.ts"
      via: "useReadingStore for last-read position"
      pattern: "useReadingStore"
---

<objective>
Build the complete home screen with surah list, juz list, tab switching, search filtering, resume reading FAB, and navigation to the Quran reader. This is the primary entry point of the app after onboarding.

Purpose: Users need to browse and find surahs quickly (QTEXT-02) with a polished, reverent design (UI-01). The home screen is the main navigation hub.

Output: Fully functional home screen with all interactive components, backed by real SQLite data.
</objective>

<execution_context>
@/Users/ahmeddaraz/VibeCoding/tasmi/.claude/get-shit-done/workflows/execute-plan.md
@/Users/ahmeddaraz/VibeCoding/tasmi/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-foundation-and-quran-display/01-CONTEXT.md
@.planning/phases/01-foundation-and-quran-display/01-UI-SPEC.md
@.planning/phases/01-foundation-and-quran-display/01-RESEARCH.md
@.planning/phases/01-foundation-and-quran-display/01-01-SUMMARY.md

<interfaces>
<!-- Types and hooks created in Plan 01 that this plan consumes -->

From src/data/types.ts:
```typescript
export interface Surah {
  number: number;
  nameArabic: string;
  nameEnglish: string;
  ayahCount: number;
  revelationType: 'Makki' | 'Madani';
  revelationOrder: number;
  juzStart: number;
}

export interface Juz {
  number: number;
  startSurah: number;
  startAyah: number;
  endSurah: number;
  endAyah: number;
}

export interface LastReadPosition {
  surahNumber: number;
  ayahNumber: number;
  scrollOffset: number;
}
```

From src/hooks/useSurahList.ts:
```typescript
export function useSurahList(): { surahs: Surah[]; loading: boolean; error: string | null }
```

From src/hooks/useJuzList.ts:
```typescript
export function useJuzList(): { juzList: Juz[]; loading: boolean; error: string | null }
```

From src/hooks/useSearch.ts:
```typescript
export function useSearch(surahs: Surah[], debounceMs?: number): { query: string; setQuery: (q: string) => void; filtered: Surah[] }
```

From src/stores/readingStore.ts:
```typescript
export const useReadingStore: {
  lastReadSurah: number | null;
  lastReadAyah: number | null;
  hasCompletedOnboarding: boolean;
}
```

From src/constants/theme.ts:
```typescript
export const theme: {
  colors: { primary: '#0D7377'; accent: '#C9A84C'; background: '#FAF8F2'; surface: '#FFFFFF'; text: '#1A1A2E'; textSecondary: '#6B7280'; textDisabled: '#9CA3AF'; divider: '#E5E2DA'; };
  spacing: { xs: 4; sm: 8; md: 16; lg: 24; xl: 32; };
  typography: { label: { size: 14 }; body: { size: 18 }; heading: { size: 24; weight: '700' }; display: { size: 28 } };
  fonts: { arabic: 'KFGQPC-Uthmani' };
}
```

From src/utils/arabic.ts:
```typescript
export function toArabicIndic(num: number): string;
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: SurahListItem, JuzListItem, SearchBar, TabBar, and utility UI components</name>
  <files>
    src/components/home/SurahListItem.tsx, src/components/home/JuzListItem.tsx,
    src/components/home/SearchBar.tsx, src/components/home/TabBar.tsx,
    src/components/home/ResumeReadingFAB.tsx,
    src/components/ui/EmptySearchResult.tsx, src/components/ui/ErrorState.tsx
  </files>
  <read_first>
    .planning/phases/01-foundation-and-quran-display/01-UI-SPEC.md (component inventory, interaction details, SurahListItem layout, copywriting contract, color reserved-for lists, accessibility requirements),
    src/data/types.ts (Surah and Juz interfaces),
    src/constants/theme.ts (color values and typography tokens),
    src/utils/arabic.ts (toArabicIndic function),
    src/stores/readingStore.ts (useReadingStore for FAB)
  </read_first>
  <action>
    **Create `src/components/home/SurahListItem.tsx`:**

    Props: `{ surah: Surah; onPress: (surahNumber: number) => void; isActive?: boolean }`

    Layout per UI-SPEC "SurahListItem layout" section:
    - Left side: Surah number inside a 36x36px diamond shape (45-degree rotated square). Diamond has 1px gold (#C9A84C) border, transparent fill. Number text centered inside in Label size (14px), primary text color (#1A1A2E).
    - Center: Top line — surah.nameArabic in Heading size (24px), weight 700, fontFamily 'KFGQPC-Uthmani', color #1A1A2E, with `writingDirection: 'rtl'`. Bottom line — surah.nameEnglish in Label size (14px), weight 400, color #6B7280.
    - Right side: "{ayahCount} ayat" in Label size (14px), color #6B7280 on top. surah.revelationType in Label size (14px), color #6B7280 on bottom. Right-aligned.
    - Minimum height: 72px, vertically centered content.
    - Bottom divider: 1px #E5E2DA, indented 16px from left.
    - Pressed state: background opacity change (use Pressable with `({ pressed }) => [...]` pattern).
    - Active state (isActive=true): surah.nameArabic color changes to #0D7377 (teal).
    - Accessibility: `accessibilityLabel={`Surah ${surah.nameEnglish}, ${surah.ayahCount} ayahs, ${surah.revelationType}`}`
    - Touch target: minimum 44px height (already satisfied by 72px).

    **Create `src/components/home/JuzListItem.tsx`:**

    Props: `{ juz: Juz; surahNames: Map<number, string>; onPress: (juzNumber: number) => void }`

    Layout:
    - Left side: Juz number in Arabic-Indic numerals (use toArabicIndic) inside a 36x36px circle with 1px gold (#C9A84C) border.
    - Center: "Juz {arabicIndicNumber}" as heading. Below: "Starts at Surah {surahName}, Ayah {startAyah}" in Label size, #6B7280.
    - Minimum height: 64px.
    - Bottom divider: 1px #E5E2DA, indented 16px from left.
    - Pressed state: same as SurahListItem.

    **Create `src/components/home/SearchBar.tsx`:**

    Props: `{ value: string; onChangeText: (text: string) => void; placeholder?: string }`

    Layout per UI-SPEC:
    - TextInput with white (#FFFFFF) background, 12px border-radius, 16px horizontal padding, 44px height.
    - Left icon: Ionicons 'search-outline', color #9CA3AF, size 20.
    - Placeholder text: "Search surahs..." (or Arabic per locale), color #9CA3AF.
    - Focus state: 2px border #0D7377 (teal).
    - Unfocused state: 1px border #E5E2DA.
    - Clear button (X icon) on right when value is non-empty.
    - 16px vertical margin (top and bottom).

    **Create `src/components/home/TabBar.tsx`:**

    Props: `{ activeTab: 'surah' | 'juz'; onTabChange: (tab: 'surah' | 'juz') => void }`

    Layout per UI-SPEC:
    - Two-segment control: "Surah" and "Juz" (or Arabic equivalents per locale).
    - Active segment: #0D7377 (teal) background, white text, rounded corners.
    - Inactive segment: transparent background, #6B7280 text.
    - Container: #F3F4F6 background pill shape, 4px padding, full width minus 32px horizontal margin.
    - 8px below SearchBar.
    - Each segment: 44px height minimum touch target.

    **Create `src/components/home/ResumeReadingFAB.tsx`:**

    Props: `{ onPress: () => void }`

    Layout per UI-SPEC:
    - Floating action button positioned bottom-right, 16px from right edge, 16px above bottom tab bar.
    - Circular, 56px diameter, #0D7377 (teal) background.
    - Icon: MaterialCommunityIcons 'book-open-page-variant', white, size 24.
    - Shadow: elevation 4 (Android), shadow properties (iOS).
    - Only visible when useReadingStore has a lastReadSurah !== null. Import useReadingStore and check inside component.
    - Pressed state: background darkens to #0B6163.
    - accessibilityLabel="Continue reading"

    **Create `src/components/ui/EmptySearchResult.tsx`:**

    Props: none (or optional `query: string`)

    Layout per UI-SPEC copywriting contract:
    - Centered vertically in available space.
    - Icon: Ionicons 'search-outline', #9CA3AF, size 48.
    - Heading: "No surahs found" in Body size (18px), #1A1A2E, centered.
    - Body: "Try searching by surah name or number." in Label size (14px), #6B7280, centered.

    **Create `src/components/ui/ErrorState.tsx`:**

    Props: `{ message?: string; onRetry: () => void }`

    Layout per UI-SPEC:
    - Centered vertically and horizontally, full screen.
    - Icon: MaterialCommunityIcons 'alert-circle-outline', #DC2626, size 48.
    - Heading: message or default "Unable to load Quran text. Please restart the app." in Body size (18px), #1A1A2E, centered.
    - Button: "Try Again", #0D7377 background, white text, 44px height, 16px horizontal padding, 8px border-radius.
  </action>
  <verify>
    <automated>npx jest --passWithNoTests</automated>
  </verify>
  <acceptance_criteria>
    - src/components/home/SurahListItem.tsx contains 'accessibilityLabel' and '#C9A84C' (gold diamond) and 'KFGQPC-Uthmani' and 'onPress'
    - src/components/home/SurahListItem.tsx contains '72' (min height) and '#E5E2DA' (divider) and 'Pressable'
    - src/components/home/JuzListItem.tsx contains 'toArabicIndic' and '#C9A84C' and 'onPress'
    - src/components/home/SearchBar.tsx contains 'search-outline' and '#0D7377' and 'TextInput' and '#9CA3AF'
    - src/components/home/TabBar.tsx contains "'surah'" and "'juz'" and '#0D7377' and 'onTabChange'
    - src/components/home/ResumeReadingFAB.tsx contains 'useReadingStore' and 'book-open-page-variant' and '#0D7377' and '56'
    - src/components/ui/EmptySearchResult.tsx contains 'No surahs found' and 'search-outline'
    - src/components/ui/ErrorState.tsx contains 'Try Again' and '#DC2626' and 'onRetry'
  </acceptance_criteria>
  <done>
    All home screen components are created with exact UI-SPEC styling, proper accessibility labels, correct color usage (teal for interactive, gold for ornamental), and proper touch targets.
  </done>
</task>

<task type="auto">
  <name>Task 2: Home screen assembly with FlashList, tabs, search, and navigation wiring</name>
  <files>
    app/(tabs)/index.tsx, app/(tabs)/_layout.tsx
  </files>
  <read_first>
    .planning/phases/01-foundation-and-quran-display/01-UI-SPEC.md (Home screen layout wireframe, loading states, bottom tab bar spec),
    .planning/phases/01-foundation-and-quran-display/01-RESEARCH.md (FlashList pattern, RTL notes),
    src/components/home/SurahListItem.tsx (component props),
    src/components/home/JuzListItem.tsx (component props),
    src/components/home/SearchBar.tsx (component props),
    src/components/home/TabBar.tsx (component props),
    src/components/home/ResumeReadingFAB.tsx (component props),
    src/components/ui/EmptySearchResult.tsx (component usage),
    src/components/ui/ErrorState.tsx (component usage),
    src/hooks/useSurahList.ts (hook return type),
    src/hooks/useJuzList.ts (hook return type),
    src/hooks/useSearch.ts (hook return type),
    src/stores/readingStore.ts (store shape),
    src/data/types.ts (Surah and Juz types),
    app/(tabs)/_layout.tsx (current placeholder)
  </read_first>
  <action>
    **Update `app/(tabs)/index.tsx` — Full home screen implementation:**

    1. Import and use hooks: `useSurahList()`, `useJuzList()`, `useSearch(surahs)`.
    2. Import and use components: SurahListItem, JuzListItem, SearchBar, TabBar, ResumeReadingFAB, EmptySearchResult, ErrorState.
    3. Local state: `activeTab: 'surah' | 'juz'` (default 'surah').
    4. Screen layout (top to bottom):
       - Screen title "Tasmi'" in Heading size (24px), weight 700, color #1A1A2E, left-aligned with 32px horizontal padding, 16px top padding.
       - SearchBar — bound to useSearch hook's query/setQuery. Only visible when activeTab is 'surah'.
       - TabBar — bound to activeTab state.
       - When activeTab === 'surah':
         - FlashList rendering `filtered` surahs (from useSearch hook).
         - `renderItem` renders SurahListItem with `onPress` navigating via `router.push(`/surah/${surah.number}`)`.
         - `estimatedItemSize={72}` (SurahListItem min height).
         - `ListEmptyComponent` renders EmptySearchResult when filtered is empty and query is non-empty.
       - When activeTab === 'juz':
         - FlashList rendering `juzList` from useJuzList hook.
         - `renderItem` renders JuzListItem. Need to build a surahNames map from surahs data: `new Map(surahs.map(s => [s.number, s.nameArabic]))`.
         - `onPress` navigates to the first surah of the juz: `router.push(`/surah/${juz.startSurah}`)`.
         - `estimatedItemSize={64}` (JuzListItem min height).
    5. Loading state: Show a simple ActivityIndicator centered on screen while useSurahList is loading.
    6. Error state: Show ErrorState component with retry callback that re-triggers data fetch.
    7. ResumeReadingFAB positioned absolutely, bottom-right, with `onPress` navigating to `router.push(`/surah/${lastReadSurah}`)`.
    8. Background color: #FAF8F2 (cream) on the outer container.
    9. Use SafeAreaView for proper insets.

    **Verify `app/(tabs)/_layout.tsx` matches UI-SPEC BottomTabBar spec:**

    Ensure the tab layout created in Plan 01 has:
    - Active tint: #0D7377
    - Inactive tint: #9CA3AF
    - Tab bar background: #FFFFFF
    - Top border: 1px #E5E2DA
    - Height: 56px (tabBarStyle.height)
    - Home icon: MaterialCommunityIcons 'home'
    - Settings icon: MaterialCommunityIcons 'cog'
    - Header shown: false (hide default header, home screen has its own title)

    If any of these values are wrong in the Plan 01 placeholder, update them now.
  </action>
  <verify>
    <automated>npx jest --passWithNoTests</automated>
  </verify>
  <acceptance_criteria>
    - app/(tabs)/index.tsx contains 'useSurahList' and 'useSearch' and 'useJuzList'
    - app/(tabs)/index.tsx contains 'FlashList' import from '@shopify/flash-list'
    - app/(tabs)/index.tsx contains 'SurahListItem' and 'JuzListItem' and 'SearchBar' and 'TabBar' and 'ResumeReadingFAB'
    - app/(tabs)/index.tsx contains "router.push" with "/surah/" path
    - app/(tabs)/index.tsx contains 'EmptySearchResult' and 'ErrorState'
    - app/(tabs)/index.tsx contains '#FAF8F2' or 'cream' in background styling
    - app/(tabs)/index.tsx contains "estimatedItemSize" prop on FlashList
    - app/(tabs)/_layout.tsx contains '#0D7377' and '#9CA3AF' and '#FFFFFF' and '#E5E2DA' and '56'
    - app/(tabs)/_layout.tsx contains 'headerShown' set to false
  </acceptance_criteria>
  <done>
    Home screen shows all 114 surahs in a FlashList, supports switching between Surah and Juz tabs, filters surahs via search bar with 300ms debounce, shows empty state when no results match, displays error state with retry on data load failure, and has a resume reading FAB that navigates to last-read surah. Bottom tab bar matches UI-SPEC exactly.
  </done>
</task>

</tasks>

<verification>
- Home screen renders surah list from SQLite data
- Tapping a surah navigates to /surah/[id] route
- Search filtering works (type "baqara" -> shows Al-Baqarah)
- Tab switching between Surah and Juz lists works
- Resume reading FAB appears only when last-read position exists
- All colors match UI-SPEC (teal for interactive, gold for ornamental, cream background)
- All text sizes match typography scale
- `npx jest --passWithNoTests` passes
</verification>

<success_criteria>
- User can see all 114 surahs on the home screen
- User can switch between Surah and Juz browsing tabs
- User can search/filter surahs by name or number
- User can tap a surah to navigate to the reader (placeholder)
- Resume reading FAB navigates to last-read surah when position exists
- All components follow UI-SPEC visual design exactly
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-and-quran-display/01-02-SUMMARY.md`
</output>
